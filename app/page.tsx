import { Workspace } from '@containers';
// TODO this is left here until we create a landing page
import { Homepage } from '@containers';

export default function Home() {

  return (
    <main className="w-full">
      <Homepage />
    </main>
  );
}
